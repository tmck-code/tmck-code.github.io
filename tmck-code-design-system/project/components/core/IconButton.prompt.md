A square icon-only control for toolbars, terminal window chrome and card corners. Always pass `label`.

```jsx
<IconButton icon="copy" label="Copy command" />
<IconButton icon="settings" label="Settings" variant="solid" active />
```
