The default surface container. Border, not shadow — shadows are for real elevation only.

```jsx
<Card eyebrow="Go" title="pokesay" footer="★ 33 · forked 3" interactive>
  <p>Print pokemon in the CLI! An adaptation of the classic "cowsay"</p>
</Card>
```

`featured` adds the amber border and hard ink offset shadow — hero slot only.
