A repo listing card. Names stay lowercase and hyphenated; descriptions stay one plain line.

```jsx
<RepoCard name="pokesay" description='Print pokemon in the CLI! An adaptation of the classic "cowsay"' language="Go" stars={33} forks={3} />
```

Language dots use the brand palette, not GitHub's. `featured` = amber border + hard shadow, one per grid.
