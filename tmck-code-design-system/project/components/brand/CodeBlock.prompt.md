Inline code sample with a copy button. Use inside posts and READMEs; use TerminalWindow when you're showing *output* rather than source.

```jsx
<CodeBlock label="install" code="pip install laser-prynter" />
```
