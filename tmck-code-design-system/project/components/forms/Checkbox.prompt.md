Controlled checkbox; `onChange` receives the boolean first.

```jsx
<Checkbox label="Show ANSI preview" checked={on} onChange={setOn} />
```

The tick is the Unicode ✓ in JetBrains Mono, not an SVG — that's deliberate (see ICONOGRAPHY).
