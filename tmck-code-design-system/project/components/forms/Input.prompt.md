Text field on an inset dark surface with an amber focus ring.

```jsx
<Input label="Repository" prefix="github.com/" mono placeholder="tmck-code/pokesay" />
```

Use `mono` for anything the user would type into a terminal. `error` overrides `hint`.
