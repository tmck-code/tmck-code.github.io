import { BubblePlotDatasetTransition } from "./BubblePlotWithTransition.tsx";

export default function BubblePlotDatasetTransitionDemo(width: number, height: number) {
  return (
    <BubblePlotDatasetTransition width={width} height={height} />
  );
}
